<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js" integrity="sha512-v2CJ7UaYy4JwqLDIrZUI/4hqeoQieOmAZNXBeQyjo21dadnwR+8ZaIJVT8EE2iyI61OV8e6M8PP2/4hpQINQ/g==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .forgot-password-container {
            display: flex;
            width: 90%;
            max-width: 600px;
            height: 49vh;
            border-radius: 10px;
            overflow: hidden;
            background-color: #fff;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }

        .form-section {
            flex: 1;
            padding: 40px;
            height: 59%;
        }

        .form-section h1 {
            font-size: 24px;
            color: #333;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 15px;
            position: relative;
        }

        .form-group label {
            font-size: 14px;
            color: #555;
            display: block;
            margin-bottom: 5px;
        }

        .form-group input {
            width: 56%;
            padding: 10px 40px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 14px;
            color: #333;
        }

        .form-group input:focus {
            border-color: #007bff;
            outline: none;
            box-shadow: 0 0 3px rgba(0, 123, 255, 0.25);
        }

        .form-group i {
            position: absolute;
            top: 50%;
            left: 10px;
            transform: translateY(-50%);
            font-size: 18px;
            color: #999;
        }

        .reset-btn {
            background-color: #007bff;
            color: #ffffff;
            border: none;
            padding: 10px 15px;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            width: 89%;
            margin-top: 20px;
        }

        .reset-btn:hover {
            background-color: #0056b3;
        }

        .image-section {
            flex: 1;
            background: url('/user/assets/sofa-1.1.jpg') center center/cover no-repeat;
            height: 100%;
        }

        .a-tag {
            font-size: 12px;
            text-decoration: none;
            display: block;
            margin-top: 10px;
            color: #007bff;
            text-align: center;
        }

        .a-tag:hover {
            text-decoration: underline;
        }
        .icon{
            margin-top: 8px;
        }
    </style>
</head>
<body>
    <div class="forgot-password-container">
       
        <div class="form-section">
            <h1>Forgot Password</h1>
            <p>Enter your email address to receive password reset instructions.</p>
            <form id="forgotPasswordForm" action="/user/forgetpassword" method="post">
                
                <div class="form-group">
                    <label for="email">Email</label>
                    <i class="bi bi-envelope icon"></i>
                    <input type="email" id="email" name="email" placeholder="Enter your email" required>
                </div>
                <button type="submit" class="reset-btn">Send Reset Instructions</button>
                <a href="/user/login" class="a-tag">Back to Login</a>
            </form>
        </div>
       
        <div class="image-section"><center><% if (typeof message !== 'undefined' && message) { %> 
            <p style="color: red; margin-top: 200px; font-family: Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;"><%= message %></p> 
        <% } %></center> </div>
    </div>
</body>
</html>
